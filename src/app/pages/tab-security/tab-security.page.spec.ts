/*
import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core'
import { async, ComponentFixture, TestBed } from '@angular/core/testing'

import { TabSecurityPage } from './tab-security.page'

describe('TabSecurityPage', () => {
  let component: TabSecurityPage
  let fixture: ComponentFixture<TabSecurityPage>

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [TabSecurityPage],
      schemas: [CUSTOM_ELEMENTS_SCHEMA]
    }).compileComponents()
  }))

  beforeEach(() => {
    fixture = TestBed.createComponent(TabSecurityPage)
    component = fixture.componentInstance
    fixture.detectChanges()
  })

  it('should create', () => {
    expect(component).toBeTruthy()
  })
})
*/
