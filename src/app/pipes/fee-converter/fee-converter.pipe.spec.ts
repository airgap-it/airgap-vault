import { FeeConverterPipe } from './fee-converter.pipe'

describe('FeeConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new FeeConverterPipe()
    expect(pipe).toBeTruthy()
  })
})
